// lucide-react (this app's icon set everywhere else) deliberately ships no
// brand/social-logo icons — it dropped them when it forked from Feather
// Icons. These two are hand-added, matching lucide's own stroke
// conventions (2px stroke, round caps/joins, 24x24 viewbox, currentColor)
// so they sit visually consistent with every <Mail>/<Eye>/etc. icon used
// alongside them, rather than pulling in a whole extra icon-pack
// dependency for two glyphs. Paths are Feather Icons' original
// facebook/instagram marks (MIT licensed) — lucide's own icon set before
// brand icons were removed.
type IconProps = { size?: number; className?: string };

export function FacebookIcon({ size = 16, className }: IconProps) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
      aria-hidden="true"
    >
      <path d="M18 2h-3a5 5 0 0 0-5 5v3H7v4h3v8h4v-8h3l1-4h-4V7a1 1 0 0 1 1-1h3z" />
    </svg>
  );
}

export function InstagramIcon({ size = 16, className }: IconProps) {
  return (
    <svg
      width={size}
      height={size}
      viewBox="0 0 24 24"
      fill="none"
      stroke="currentColor"
      strokeWidth={2}
      strokeLinecap="round"
      strokeLinejoin="round"
      className={className}
      aria-hidden="true"
    >
      <rect x="2" y="2" width="20" height="20" rx="5" ry="5" />
      <path d="M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z" />
      <line x1="17.5" y1="6.5" x2="17.51" y2="6.5" />
    </svg>
  );
}
